{
  "name": "grenoble-hands-on-hotwire",
  "version": "1.0.0",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "deploy": "cp build/src/*.js ../resources/static/js && cp build/src/*.css ../resources/static/css && cp -r build/_snowpack ../resources/static/",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.{ts,js}\"",
    "lint": "prettier --check \"src/**/*.{ts,js}\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hotwired/turbo": "^7.1.0",
    "@snowpack/plugin-sass": "^1.4.0",
    "bulma": "^0.9.3",
    "stimulus": "^2.0.0"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "snowpack": "^3.0.11"
  }
}
